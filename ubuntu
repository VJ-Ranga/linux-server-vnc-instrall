$ sudo apt update
$ sudo apt install xfce4 xfce4-goodieshttps://github.com/VJ-Ranga/linux-server-vnc-instrall/blob/main/ubuntu
$ sudo apt install tightvncserver
$ vncserver
$ vncserver -kill :1
$ mv ~/.vnc/xstartup ~/.vnc/xstartup.bak
$ nano ~/.vnc/xstartup
(paste the follow being in the xstartup file and write out)

#!/bin/bash
xrdb $HOME/.Xresources
startxfce4 &

$ sudo chmod +x ~/.vnc/xstartup
$ vncserver

(change resolution)
$ vncserver -kill :1
$ vncserver -geometry 1366x768
